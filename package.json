{
  "name": "nodejs.nginx",
  "version": "0.1.0",
  "scripts": {
    "start": "node app.js & cd nginx-1.18.0 && start nginx",
    "reload": "node app.js & cd nginx-1.18.0 && ./nginx.exe -s reload"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
